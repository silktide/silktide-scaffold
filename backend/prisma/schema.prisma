// Prisma Schema — single source of truth for the database.
//
// To customize:
//   1. Add your models below.
//   2. Run `npm run db:generate -w backend` to regenerate the client.
//   3. Run `npm run db:migrate` to create and apply a migration.

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}
