# Database (PostgreSQL) — optional. Leave commented out to run without a database.
# DATABASE_URL=postgresql://myuser:mypassword@localhost:5432/mydb

BACKEND_PORT=4000
FRONTEND_URL=http://localhost:3000
NODE_ENV=development
